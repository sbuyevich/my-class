# UI Changes

- Add a Teacher Home menu item for teachers.
- The Teacher Home page should use the existing teacher landing content unless a new route is required.
- Keep the teacher menu drawer open after teacher login.
- Fix the Teacher Quiz Start button so clicking Start begins the selected quiz/question and updates student screens.
- On the Student page, do not show quiz answer numbers or answer buttons before a quiz question is active.
- Make the app header responsive on mobile so title, greeting, class, and school text do not overflow or crowd each other.
- Remove the local `Student` page title and class subtitle from the Student page content. The shared header can continue showing class context.